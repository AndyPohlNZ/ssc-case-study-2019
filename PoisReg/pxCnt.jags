model{

# Likelihood
for(i in 1:N){
  y[i] ~ dpois(lambda[i])
  log(lambda[i]) <- eta[i]
  eta[i] <- beta0 + beta1*x[i]
}

# prior
beta0 ~ dnorm(0, 0.0001)
beta1 ~ dnorm(0, 0.0001)


### Prediction

for(j in 1:NTest){
  y_hat[j] ~ dpois(lambda_hat[j])
  log(lambda_hat[j]) <- eta_hat[j]
  eta_hat[j] <- beta0 + beta1*xtest[j]
}


}