model{

# Likelihood
for(i in 1:N){
  y[i] ~ dpois(lambda[i])
  log(lambda[i]) <- eta[i]
  eta[i] <- beta1*x[i] + beta2*x2[i]
}

# prior
beta1 ~ dnorm(0, 0.0001)
beta2 ~ dnorm(0, 0.0001)

### Prediction

for(j in 1:NTest){
  y_hat[j] ~ dpois(lambda_hat[j])
  log(lambda_hat[j]) <- eta_hat[j]
  eta_hat[j] <- beta1*xtest[j] +beta2*xtest2[j]
}


}